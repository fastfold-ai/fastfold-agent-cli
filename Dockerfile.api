FROM python:3.11-slim

WORKDIR /app

COPY pyproject.toml README.md ./
COPY src/ src/

RUN pip install --no-cache-dir -e ".[api]"

EXPOSE 8000

# Mount data directory
VOLUME /data
ENV CT_DATA_ROOT=/data

CMD ["uvicorn", "ct.api.app:app", "--host", "0.0.0.0", "--port", "8000"]
